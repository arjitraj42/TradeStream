{
  "name": "trading-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix Frontend run dev",
    "build": "npm --prefix Frontend run build && (cd backend && npx prisma generate --schema=prisma/schema.prisma)",
    "build:frontend": "npm --prefix Frontend run build",
    "build:backend": "cd backend && npx prisma generate --schema=prisma/schema.prisma",
    "prisma:generate": "cd backend && npx prisma generate --schema=prisma/schema.prisma",
    "preview": "npm --prefix Frontend run preview"
  }
}
